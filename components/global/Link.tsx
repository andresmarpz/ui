import { styled } from '@/stitches.config';

const StyledLink = styled('a', {
    color: '$textHighlight',
    textDecoration: 'none',
    fontWeight: 500,
    lineHeight: 1.5
});

export default StyledLink;
